:- ['semantics.pro'].
:- ['closure.pro'].
:- ['system.pro'].

/*

Try these goals, for example:

?- step(name(system),A,E). % a single action  

?- many(2,name(system),L,E). % action sequences of length 2

*/
